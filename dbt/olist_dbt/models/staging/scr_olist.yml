version: 2

sources:
  - name: olist_raw
    description: "Raw Olist E-commerce data loaded from CSVs via Meltano"
    database: my-project-sctp-module-2  # your project ID
    schema: olist_raw                   # Bigquery Dataset
    tables:
      - name: raw_customers
      - name: raw_geolocation
      - name: raw_order_items
      - name: raw_payments
      - name: raw_products
      - name: raw_reviews
      - name: raw_sellers
      - name: raw_orders
      - name: raw_category_translation